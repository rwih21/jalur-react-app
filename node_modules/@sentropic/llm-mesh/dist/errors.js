const defaultRetryableStatusCodes = [408, 409, 425, 429, 500, 502, 503, 504];
const defaultRetryableCodes = [
    'rate_limit',
    'rate_limit_exceeded',
    'timeout',
    'server_error',
    'overloaded',
    'temporarily_unavailable',
];
const asRecord = (value) => {
    return value && typeof value === 'object' ? value : null;
};
const readString = (record, key) => {
    const value = record?.[key];
    return typeof value === 'string' && value.trim() ? value : undefined;
};
const readNumber = (record, key) => {
    const value = record?.[key];
    return typeof value === 'number' && Number.isFinite(value) ? value : undefined;
};
const nestedErrorRecord = (record) => {
    return asRecord(record?.error) || asRecord(record?.response);
};
const inferRetryReason = (statusCode, code) => {
    const normalizedCode = code?.toLowerCase();
    if (statusCode === 429 || normalizedCode?.includes('rate'))
        return 'rate_limit';
    if (statusCode === 408 || normalizedCode?.includes('timeout'))
        return 'timeout';
    if (statusCode === 409 || statusCode === 425 || normalizedCode?.includes('overload')) {
        return 'overloaded';
    }
    if (typeof statusCode === 'number' && statusCode >= 500)
        return 'server_error';
    if (normalizedCode?.includes('network') || normalizedCode === 'fetch_error')
        return 'network';
    return undefined;
};
export const isRetryableProviderError = (statusCode, code, options = {}) => {
    const retryableStatusCodes = options.retryableStatusCodes ?? defaultRetryableStatusCodes;
    const retryableCodes = options.retryableCodes ?? defaultRetryableCodes;
    const nonRetryableCodes = options.nonRetryableCodes ?? [];
    if (code && nonRetryableCodes.includes(code))
        return false;
    if (typeof statusCode === 'number' && retryableStatusCodes.includes(statusCode))
        return true;
    if (!code)
        return false;
    const normalizedCode = code.toLowerCase();
    return retryableCodes.some((retryableCode) => normalizedCode.includes(retryableCode));
};
export const normalizeProviderError = (providerId, error, options = {}) => {
    const record = asRecord(error);
    const nested = nestedErrorRecord(record);
    const statusCode = readNumber(record, 'statusCode') ||
        readNumber(record, 'status') ||
        readNumber(nested, 'statusCode') ||
        readNumber(nested, 'status');
    const code = readString(record, 'code') ||
        readString(record, 'type') ||
        readString(nested, 'code') ||
        readString(nested, 'status');
    const message = readString(record, 'message') ||
        readString(nested, 'message') ||
        (error instanceof Error && error.message) ||
        options.defaultMessage ||
        `${providerId} request failed`;
    const retryAfterSeconds = readNumber(record, 'retryAfter') || readNumber(nested, 'retryAfter');
    const retryAfterMs = readNumber(record, 'retryAfterMs') ||
        readNumber(nested, 'retryAfterMs') ||
        (typeof retryAfterSeconds === 'number' ? retryAfterSeconds * 1000 : undefined);
    const retryable = isRetryableProviderError(statusCode, code, options);
    const retryReason = retryable ? inferRetryReason(statusCode, code) ?? 'unknown' : undefined;
    return {
        providerId,
        message,
        retryable,
        ...(code ? { code } : {}),
        ...(retryReason ? { retryReason } : {}),
        ...(retryAfterMs ? { retryAfterMs } : {}),
        ...(typeof statusCode === 'number' ? { statusCode } : {}),
        ...(error instanceof Error || record ? { cause: error } : {}),
    };
};
//# sourceMappingURL=errors.js.map